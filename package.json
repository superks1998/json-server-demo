{
    "name": "json_server",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "npm run generate-data && node main.js",
        "dev": "npm run generate-data && nodemon main.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "generate-data": "node generate-data.js"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "faker": "^5.5.3",
        "json-server": "^0.16.3",
        "query-string": "^7.0.1"
    },
    "devDependencies": {
        "nodemon": "^2.0.12"
    }
}
